import React from 'react';

const RedirectorInfo = (props) => {
    return(
        <div className="page__redirector-info">
            {props.children}
        </div>
    );
}

export default RedirectorInfo;